The builder tool is a Docker Container Image that encapsulates the necessary code and dependencies to perform a single, critical MLOps task: triggering a Vertex AI Pipeline run.

cloudvuild - 
Build your custom Docker tool called pipeline-executor.
That’s a reusable program that knows how to talk to Vertex AI. 
. 